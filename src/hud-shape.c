#include "hud/shape.h"

#include <stdlib.h>

Simple_Shape* push_back(Simple_Shape value) {
    Simple_Shape* tail = malloc(sizeof(Simple_Shape));

    if (!tail) {

        return NULL;
    }



    return tail;
}
